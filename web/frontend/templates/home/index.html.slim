.offers
  h1.content-subhead Pair programming sessions
  - offers.each do |offer|
    == render partial: 'home/offer', locals: { offer: offer }

javascript:
  $(function(){
    $('form.request-create button[type=submit]').one('click', function(event){
      $(this).parent('form').find('fieldset').show();
      $(this).addClass('pure-button-primary');
      event.preventDefault();
      return false;
    });
  });

